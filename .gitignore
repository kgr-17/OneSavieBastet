# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.env
.env.*

# Local Claude / IDE state
.claude/
.vscode/
.idea/
*.swp
.DS_Store

# Agent bridge — local async coordination, not for public repo
bridges/

# Artifacts (regenerable). NOTE: outputs/ (submission CSVs) tracked for backup — private repo.
artifacts/

# Logs
*.log

# Competition data (multi-GB, sourced from Azure blob URLs in download-vuln.ipynb)
*.zip
data/
download-vuln.ipynb
teamate_hightst_submission.csv

# Scratch / sc2026-06 dev session (regenerable LLM output dumps)
tmp_*.txt
tmp_*.json
out*.txt
0d40f2c3.csv
temp_*.txt
tools/_*.txt
scratch/
tools/renft_src/

# Labeling handoff — teammate deliverable + internal eval (shared via zip, not repo)
labeling_handoff/
labeling_handoff_local/

# Fine-tuning experiment: keep the big regenerable LLM input-batch dumps out, but
# track the small valuable artifacts (mappings, predictions, score ladder). Private repo.
finetune/teacher/*batches*/
finetune/data/
kaggle_ft/dataset/
finetune/results_*.json
